To install client:

 perl Makefile.PL
 make
 make test
 make install

To install server move the content of server directory to P21 server,
manually modify xaosrv.pl to include appropriate path information and
run the server. There is no automated installation step for the server.

In case of questions mail Eugene Karpachov <jk@xao.com>
